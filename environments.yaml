name: deepflightplan
channels:
  - conda-forge
dependencies:
  #
  - python=3.11
  - pandas
  - tqdm
  - matplotlib
  - pyyaml
  - pip
  - pip:
    - einops
    - geopy
    - networkx[default]
    - tabulate
    - geojson